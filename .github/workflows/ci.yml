name: kit-check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  installer:
    name: Validate role installer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash -n scripts/install-role.sh
